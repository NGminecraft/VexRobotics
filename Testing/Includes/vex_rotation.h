/**
 * VEX Rotation Sensor Header (Mock Implementation)
 */

#ifndef VEX_ROTATION_H
#define VEX_ROTATION_H

#include "vex.h"

// This header is typically included separately in VEX projects
// The rotation class is already defined in vex.h, so this is just
// for compatibility with code that includes vex_rotation.h directly

#endif // VEX_ROTATION_H
